#include <iostream> // preprosesor directive : here cout and cin are defined
#include <fstream>// here cout and cin are declared
#include <iomanip>
using namespace std;
#include<string>
#include<cmath>
#include <iostream>

/*int main(){
    int a = 4;
    float c= 4.5;
    double d = 34;
    char r = 'y';
    cout<<"the size of int is " <<sizeof(a)<<endl;
    cout<<"the size of float is "<< sizeof(c)<<endl;
    cout<<"the size of double is "<< sizeof(d)<<endl;
    cout<<"the size of char is"<< sizeof(r)<<endl;
    return 0;
}*/

/*int main(){
char ch;
    cout<<"enter the charcter "<<endl;
    cin>>ch;
    cout<<"the ascii value of a enterd charcter is "<<int(ch); // type casting from charcter to integer type ( explicit from :where the compiler is specifed the particular datatype to casted)
    //type casting :Type casting is used to convert a variable from one data type to another. 
  return 0;
}*/
/*int main(){
    char ch = 'A';
    int asciiValue = ch; // Implicit casting
    cout << "The ASCII value of " << ch << " is " << asciiValue << endl;  // this is not directly done by a compiler  no instrunction given by compiler
    return 0;
}*/




